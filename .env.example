# ===== FRONTEND ENVIRONMENT VARIABLES =====
# For Vite, all frontend env vars must start with VITE_

# API Key for Google Gemini (Required for AI features)
VITE_GEMINI_API_KEY=AIzaSyDqIQN9ckf6412tYLbVBRNXS4wGM642RLQ

# API URL for backend (optional, defaults to http://localhost:5000/api)
VITE_API_URL=http://localhost:5000/api

# ===== BACKEND ENVIRONMENT VARIABLES =====

# Gemini API Key for backend (used by server.js)
GEMINI_API_KEY=AIzaSyDqIQN9ckf6412tYLbVBRNXS4wGM642RLQ

# Port for the backend server (default: 5000)
PORT=5000

# ===== PRODUCTION NOTES =====
# This app requires TWO deployments:
#
# 1. FRONTEND on Vercel:
#    - Set VITE_API_URL to your Render backend URL (e.g., https://gyan-backend.onrender.com/api)
#    - Set VITE_GEMINI_API_KEY for client-side AI features (if any)
#
# 2. BACKEND on Render.com:
#    - Create a "Web Service" from your GitHub repo
#    - Build Command: npm install
#    - Start Command: node server.js
#    - Set Environment Variables:
#      - GEMINI_API_KEY: your Google AI API key
#      - PORT: 5000 (or leave blank, Render assigns automatically)
#      - NODE_ENV: production
#
# IMPORTANT: The Render backend URL must be set in Vercel's VITE_API_URL
